#! /bin/bash

INSTALL_DIR=/usr/lib/pd
BUILD_DIR=.
INSTALL_CMD="cp -xR"

# s-abstractions
mkdir -p $INSTALL_DIR
mkdir -p $INSTALL_DIR/extra
mkdir -p $INSTALL_DIR/extra/s-abstractions
$INSTALL_CMD $BUILD_DIR/* $INSTALL_DIR/extra/s-abstractions
